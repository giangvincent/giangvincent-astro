---
const year = new Date().getFullYear();

const navLinks = [
	{ label: 'Portfolios', href: '/portfolio' },
	{ label: 'Services', href: '/services' },
	{ label: 'Blogs', href: '/blog' },
	{ label: 'About', href: '/about' },
];
---

<footer class="pixel-footer">
	<div class="pixel-footer__grid">
		<div class="pixel-footer__brand">
			<h4>GV LAB</h4>
			<p>
				Pixel-perfect experiments, product stories, and build logs from Vincent. Built with Astro +
				a bit of eighties nostalgia.
			</p>
		</div>
		<ul class="pixel-footer__links">
			{
				navLinks.map((link) => (
					<li>
						<a href={link.href}>{link.label}</a>
					</li>
				))
			}
		</ul>
	</div>
	<p class="pixel-footer__note">© {year} GV Lab. Transmission complete.</p>
</footer>
